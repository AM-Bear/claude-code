.PHONY: dev install build preview

dev:
	@[ -d node_modules ] || npm install
	npm run dev

install:
	npm install

build:
	@[ -d node_modules ] || npm install
	npm run build

preview:
	@[ -d node_modules ] || npm install
	npm run preview
